{% extends 'base.html.twig' %}


{% block body %}

    <form>
        <div>
            <label>Email</label>
            <input type="text" name="email">
            <button type="submit">Chercher</button>
        </div>
    </form>

    {% if user is defined %}

    {# Si le findOneBy a renvoyé un résultat #}
    {% if user is not null %}
     L'utilisateur est : {{ user }}
        {% else %}
     Aucun utilisateur avec cet email
    {% endif %}

    {% endif %}

{% endblock %}


